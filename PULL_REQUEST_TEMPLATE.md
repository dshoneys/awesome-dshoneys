# Pull Request 模板 · 插件收录申请

> 提交前请先阅读 [CONTRIBUTING.md](../CONTRIBUTING.md)。本仓库执行「**未检测不收录**」：未过安全检测的插件不会进入目录。

---

## 一、插件信息（必填）

| 字段 | 内容 |
| --- | --- |
| **插件名** | （必填，建议带版本号，如：`快捷剪贴板 v0.9.0`；示例条目请标注「虚构示例，待替换」） |
| **插件链接** | （必填，可公开访问的源码或发布页稳定链接，如 `https://github.com/<user>/<repo>`） |
| **简介** | （必填，一到两句话：解决什么问题、怎么用） |
| **作者** | （必填，群昵称 / GitHub ID / 主页均可，便于溯源与联系修复） |

## 二、dsh.so 安全检测（必填）

- [ ] 已在 [dsh.so](https://www.dsh.so/zh/submit/) 提交并取得扫描结果
- **dsh.so 插件详情页（含 slug）**：`https://www.dsh.so/zh/artifact/<slug>/`
- **dsh.so 安全扫描结果**：
- **对应插件版本/提交**：

> dsh.so 是本目录指定的独立安全检测服务。没有公开可核验的详情页和扫描结果，本 PR 不会合并进入目录。安全徽章由维护者依据结果映射，提交者无需自行判断。

## 三、群友实测反馈（选填，建议提供）

- 实测环境/场景：
- 实测结论（好不好用、有什么坑）：
- 反馈人昵称与日期：

> 暂无实测可直接写「暂无」，收录后群友可随时通过 PR 补充。

---

## 填写说明

1. **先检测**：先在 dsh.so 提交公开仓库并等待扫描完成。
2. **编辑位置**：fork 后在 `data/plugins.json` 中追加条目，`security.provider` 固定填写 `dsh.so`。
3. **链接必填**：`dshUrl` 与 `security.reportUrl` 都必须指向公开可访问的 dsh.so 页面。
4. **不要放示例**：公开目录只展示真实且已检测的插件，不接受虚构占位条目。
5. **完成后**：提交 PR，等待维护者核验数据与扫描结果；通过后自动发布到在线目录。
